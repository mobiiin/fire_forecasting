base_config: cawfe_latte_baseline.yaml

experiment:
  name: cawfe_latte_ablation_GA_separate_decoder_resblocks

cawfe_latte:
  ablation:
    name: GA_separate_decoder_resblocks
  post_fusion_backbone:
    type: residual_spatiotemporal
    num_blocks: 6
  decoder:
    type: separate_regression

training:
  output:
    root_dir: artifacts/ablations/cawfe_latte/GA_separate_decoder_resblocks
