base_config: cawfe_latte_baseline.yaml

experiment:
  name: cawfe_latte_ablation_CGA_separate_decoder_temporal_attention_resblocks

cawfe_latte:
  ablation:
    name: CGA_separate_decoder_temporal_attention_resblocks
  post_fusion_backbone:
    type: residual_spatiotemporal
    num_blocks: 6
  temporal_pooling:
    type: attention
  decoder:
    type: separate_regression

training:
  output:
    root_dir: artifacts/ablations/cawfe_latte/CGA_separate_decoder_temporal_attention_resblocks
